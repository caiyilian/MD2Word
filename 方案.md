# MD2Word 技术方案

> Markdown 转 Word，支持图片插入，精确获取页数（Windows）。

---

## 1. 技术选型

| 层 | 技术 | 说明 |
|---|---|---|
| Markdown 解析 | [mistune](https://github.com/lepture/mistune) v3 | 速度快、插件架构好、可自定义语法 |
| Word 生成 | [python-docx](https://github.com/python-openxml/python-docx) | Python 最成熟的 docx 库 |
| 页数获取 | win32com (MS Word) | Windows 平台最精确的页数获取方式 |
| CLI | argparse | 命令行入口 |

---

## 2. 架构概览

```
Markdown 输入 (.md / 字符串)
        │
        ▼
┌──────────────────────────┐
│    Parser (mistune)      │
│  Markdown → AST          │
│  插件: 图片属性, 分页符   │
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│   Document Model         │
│  dataclass 中间表示       │
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│   Renderer (python-docx) │
│  Document Model → .docx  │
│  图片处理, 样式管理       │
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│   Page Counter (win32com)│
│  打开 Word → 获取页数    │
└──────────┬───────────────┘
           ▼
    输出: .docx文件 + 页数
```

---

## 3. 核心模块设计

### 3.1 Parser — Markdown 解析

使用 mistune 将 Markdown 解析为 AST，并通过自定义插件扩展语法。

**支持的 Markdown 语法：**

| 语法 | 效果 |
|---|---|
| `# ~ ######` | 标题 1~6 级 |
| `**bold**` | 加粗 |
| `*italic*` | 斜体 |
| `` `code` `` | 行内代码 |
| ```` ``` ```` | 代码块 |
| `> quote` | 引用 |
| `- item` / `1. item` | 无序/有序列表 |
| `[link](url)` | 超链接 |
| `![alt](path)` | 图片 |
| `---` | 水平线 |
| `\| a \| b \|` | 表格 |

**扩展语法（通过 mistune 插件）：**

| 语法 | 效果 |
|---|---|
| `![](path){:width=50% align=center}` | 图片居中，宽度 50% |
| `![](path){:align=right width=300px}` | 图片右对齐，宽 300px |
| `---{pagebreak}` | 强制分页 |

**YAML Frontmatter（可选）：**

```yaml
---
title: 文档标题
author: 作者
style:
  h1:
    color: "1a5276"
    font: "Arial"
    size: 24
  code:
    bg_color: "f5f5f5"
    font: "Consolas"
---
```

### 3.2 Document Model — 中间数据模型

```python
@dataclass
class Document:
    metadata: dict
    elements: list[BlockElement]

@dataclass
class Heading(BlockElement):
    level: int
    text: str

@dataclass
class Paragraph(BlockElement):
    runs: list[InlineElement]
    alignment: str | None

@dataclass
class Image(BlockElement):
    src: str          # 本地路径
    alt: str          # 替代文本
    width: str | None  # "50%" / "300px" / None
    align: str | None  # "left" / "center" / "right"

@dataclass
class CodeBlock(BlockElement):
    language: str
    code: str

@dataclass
class Table(BlockElement):
    headers: list[str]
    rows: list[list[str]]

@dataclass
class PageBreak(BlockElement): ...
```

### 3.3 Renderer — docx 渲染

将 Document Model 逐元素渲染为 python-docx 对象。

**样式映射：**

```python
STYLE_MAP = {
    "h1":  {"style": "Heading 1", "bold": True, "size": 24},
    "h2":  {"style": "Heading 2", "bold": True, "size": 20},
    "h3":  {"style": "Heading 3", "bold": True, "size": 16},
    "code": {"font": "Consolas", "size": 9, "bg": "F5F5F5"},
    "quote": {"italic": True, "left_indent": 1.0},
}
```

**图片处理：**

```
图片来源: 仅本地文件路径
图片定位:
  - align=left   → 左对齐 (设置左对齐 + 左缩进)
  - align=center → 居中对齐 (设置居中)
  - align=right  → 右对齐 (设置右对齐)
图片尺寸:
  - "300px" → 转换为 EMU (像素 × 9525)
  - "50%"   → 计算为页面宽度的百分比
  - 省略     → 原图尺寸
```

### 3.4 Page Counter — 页数获取

使用 win32com 打开 MS Word 获取精确页数。

```python
class Win32PageCounter:
    def count(self, docx_path: str) -> int:
        word = Dispatch("Word.Application")
        word.Visible = False
        doc = word.Documents.Open(os.path.abspath(docx_path))
        pages = doc.ComputeStatistics(2)  # wdStatisticPages = 2
        doc.Close()
        word.Quit()
        return pages
```

**优点：** 与 Word 实际渲染完全一致，最精确。
**前提条件：** 系统需安装 Microsoft Word。

### 3.5 Text Enhancement — 文本格式增强 (Phase 4.5)

在现有粗体/斜体/行内代码基础上，增加更多行内格式。

**新增支持语法：**

| 语法 | 效果 | 实现方式 |
|---|---|---|
| `~~text~~` | 删除线 | mistune strikethrough 插件 |
| `<u>text</u>` / `<ins>text</ins>` | 下划线 | 解析 inline_html 标签 |
| `<sup>text</sup>` | 上标 | 解析 inline_html 标签 |
| `<sub>text</sub>` | 下标 | 解析 inline_html 标签 |
| `<span style="font-family:Arial;font-size:18px">text</span>` | 自定义字体字号 | 解析 inline_html 属性 |

> **注意**：`~text~` 和 `^text^` 语法与删除线 `~~text~~` 存在冲突，不支持。请使用 `<sub>/<sup>` 替代。

**TextRun 模型扩展：**
```python
@dataclass
class TextRun:
    text: str
    bold: bool = False
    italic: bool = False
    code: bool = False
    underline: bool = False
    strikethrough: bool = False
    superscript: bool = False
    subscript: bool = False
    font_name: Optional[str] = None  # inline 字体
    font_size: Optional[int] = None  # inline 字号 (pt)
```

### 3.6 Formula / Equation — 公式渲染 (Phase 5)

将 Markdown 中的 LaTeX 数学公式转换为 Word 原生的 OMML 公式对象。

**支持的语法：**

| 语法 | 效果 |
|---|---|
| `$...$` | 行内公式 |
| `$$...$$` | 块级公式 |
| `\begin{equation}...\end{equation}` | 带编号公式 |

**技术方案对比：**

| 方案 | 优点 | 缺点 |
|---|---|---|
| **latex2mathml → OMML** | 纯 Python，无需外部依赖 | 部分复杂语法不兼容 |
| **pandoc --math-omml** | 转换准确度高 | 需要安装 pandoc |
| **Word COM 对象模型** | 与 Word 渲染一致 | 需要安装 Word，速度慢 |

**推荐方案：latex2mathml + lxml 转 OMML**

```
Markdown ($...$, $$...$$)
       │
       ▼
Parser: 识别 $$...$$ / $...$ 公式块
       │
       ▼
LaTeX → MathML (latex2mathml)
       │
       ▼
MathML → OMML (lxml XSLT 转换)
       │
       ▼
Renderer: python-docx 插入公式 (w:oMath)
```

**依赖：**
```
latex2mathml>=3.77.0
lxml>=4.9
```

---

## 4. 项目结构

```
MD2Word/
├── md2word/
│   ├── __init__.py
│   ├── converter.py              # 主转换类
│   ├── exceptions.py             # 自定义异常
│   ├── parser/
│   │   ├── markdown_parser.py    # mistune 封装
│   │   ├── frontmatter.py        # YAML Frontmatter 解析
│   │   └── plugins/
│   │       ├── image_attr.py     # 图片属性扩展
│   │       └── pagebreak.py      # 分页符扩展
│   ├── model/
│   │   └── document.py           # 数据模型
│   ├── renderer/
│   │   ├── docx_renderer.py      # docx 渲染器
│   │   ├── styles.py             # 样式管理
│   │   └── image_handler.py      # 图片处理
│   ├── counter/
│   │   └── win32_counter.py      # 页数统计
│   └── cli/
│       └── main.py               # CLI 入口
├── tests/
│   ├── test_parser.py
│   ├── test_renderer.py
│   ├── test_converter.py
│   └── fixtures/
│       ├── sample.md
│       └── sample_image.png
├── examples/
│   ├── basic.md
│   └── with_images.md
├── requirements.txt
└── README.md
```

---

## 5. CLI 设计

```bash
# 基本用法（md → docx）
python -m md2word.cli.main input.md -o output.docx

# 获取页数
python -m md2word.cli.main input.md -o output.docx --count-pages

# 指定样式配置
python -m md2word.cli.main input.md -o output.docx --style style.yaml

# 反向转换（docx → md）
python -m md2word.cli.main input.docx -r

# 反向转换 + OCR 图片文字提取
python -m md2word.cli.main input.docx -r --ocr

# 查看帮助
python -m md2word.cli.main --help
```

Python API：

```python
from md2word import MD2Word

converter = MD2Word()
result = converter.convert_file("input.md", "output.docx")
print(f"生成文件: {result.path}")
print(f"总页数: {result.pages}")
```

---

## 6. 开发路线图 — md → docx

| 阶段 | 内容 | 说明 |
|---|---|---|
| **Phase 1** | 基础功能 | Markdown 解析 + 基础渲染（标题、段落、列表、粗斜体、代码块） |
| **Phase 2** | 图片与表格 | 图片插入（定位/尺寸）、表格渲染 |
| **Phase 3** | 页数统计 | win32com 页数获取 |
| **Phase 4** | CLI 与完善 | 命令行工具、样式自定义、测试、文档 |
| **Phase 4.5** | 文本增强 | strikethrough、underline、上标/下标、自定义字体字号 |
| **Phase 5** | 公式支持 | LaTeX 数学公式渲染为 Word 公式 |
| **Phase 6** | docx → md 基础 | 反向转换：段落、标题、列表、文本格式、水平线 |
| **Phase 7** | docx → md 图片与表格 | 图片提取、表格提取、CLI 反转模式 |
| **Phase 8** | docx ↔ md 进阶 | 超链接、代码块、公式还原、脚注、批注、分页符、OCR |
| **Phase 9** | docx → md 结构与元数据 ✅ | 元数据、页眉页脚、节属性、多级列表、CLI |
| **Phase 10** | 引用块与嵌套列表 ✅ | 双向：引用块、嵌套列表 |
| **Phase 11** | 任务列表与多级列表 ✅ | 双向：任务列表（☐/☑） |
| **Phase 12** | 页眉页脚与分栏 ✅ | 页眉页脚、页码、分栏 |
| **Phase 13** | 高级功能 | 目录、书签、文字方向、水印 |
| **Phase 14** | glm-ocr 集成 | 可选 OCR 引擎，优先 glm-ocr |

---

## 7. 开发路线图 — docx → md（反向转换）

### 概述

从 docx 读取内容，还原为 Markdown 格式，需要处理 Word 的各类元素：

```
Word (.docx) → python-docx 解析 → Document Model → Markdown 输出
```

每个 docx 文件本质是一个 ZIP 包，包含：
- `word/document.xml` — 正文内容
- `word/media/` — 嵌入的图片
- `word/header*.xml` / `word/footer*.xml` — 页眉页脚
- `word/comments.xml` — 批注
- `word/footnotes.xml` — 脚注

### Phase 6: docx → md 基础文本

- [x] 段落提取（普通段落、对齐方式）
- [x] 文本格式还原（加粗、斜体、下划线、删除线、行内代码）
- [x] 标题识别（Heading 1~9 → `#` ~ `######`）
- [x] 字体信息映射（等宽字体 → `` `code` ``）
- [x] 有序/无序列表提取（含嵌套层级）
- [x] 水平线（段落边框 → `---`）

### Phase 7: docx → md 图片与表格

- [x] 图片提取（`w:drawing` inline + anchored）
- [x] EMU → inches 尺寸转换
- [x] Alt text 提取（`wp:docPr`）
- [x] 表格提取（列对齐 `w:jc`）
- [x] CLI 反转模式输出到 `output/{name}/` 文件夹

### Phase 8: docx ↔ md 进阶（双向）

**docx → md（反向提取）：**
- [x] 超链接提取（`[text](url)`）
- [x] 代码块识别（检测等宽字体段落 → ` ``` ` 围栏代码块）
- [x] 公式还原（OMML → LaTeX，支持分数、根号、上下标、N-ary 运算符）
- [x] 脚注提取（`word/footnotes.xml` → `[^id]: text`）
- [x] 批注提取（`word/comments.xml` → `<!-- author: text -->`）
- [x] 分页符识别（`w:br w:type="page"` → `\f`）
- [x] OCR 图片文字提取（PaddleOCR → `> OCR 文本：` 块）

**md → docx（正向渲染）：**
- [x] 超链接渲染（`[text](url)` → `w:hyperlink` + relationship）
- [x] 代码块渲染（` ``` ` → Consolas 等宽字体段落）
- [x] 分页符渲染（`\pagebreak` → `w:br w:type="page"`）
- [x] 脚注渲染（`[^id]: text` → 原生 `w:footnoteReference` + 页脚文本）
- [x] 批注渲染（`<!-- author: text | target -->` → 原生 `w:commentRangeStart/End`）

**已知限制：**
- 脚注使用原生 `w:footnoteReference`，支持 Ctrl+点击跳转
- 批注使用原生 `w:commentRangeStart/End`，显示为 Word 批注气泡，支持精确目标文字
- OMML → LaTeX 转换覆盖常见模式，复杂公式可能不完整
- 超链接在反向提取时带 `<u>` 下划线（Word 默认行为）
- OCR 为可选功能，需额外安装 `paddleocr` + `paddlepaddle`

### Phase 9: docx → md 结构与元数据 ✅

- [x] 页眉/页脚提取
- [x] 文档元数据（标题、作者、创建时间）
- [x] 节属性（页面方向、页面尺寸、页边距）
- [x] 多级列表还原（从 List Bullet 2, List Bullet 3 等样式提取层级）
- [x] 样式表映射（基础支持，通过 style_mappings 参数配置）
- [x] CLI: `docx2md input.docx -o output.md`

### Phase 10: 引用块与嵌套列表（双向） ✅

**md → docx（正向渲染）：**
- [x] 引用块渲染（`> quote` → Word 引用样式段落，含左边框+缩进）
- [x] 嵌套引用（多级 `>` 嵌套）
- [x] 嵌套列表渲染（多级缩进 `  - item` → Word 多级列表样式）

**docx → md（反向提取）：**
- [x] 引用块识别（检测引用样式/左边框段落 → `> quote`）
- [x] 嵌套列表还原（从 List Bullet 2/3 样式和缩进检测提取层级）

### Phase 11: 任务列表与多级列表（双向） ✅

**md → docx（正向渲染）：**
- [x] 任务列表渲染（`- [ ] task` / `- [x] task` → Unicode 复选框 ☐/☑）
- [x] 多级列表编号渲染（从 numbering.xml 提取 lvlText 格式）

**docx → md（反向提取）：**
- [x] 任务列表识别（检测 ☐/☑ 字符 → `- [ ]` / `- [x]`）
- [x] 多级列表编号还原（从 numbering.xml 提取 lvlText 格式）

### Phase 12: 页眉页脚与分栏（双向）

**md → docx（正向渲染）：**
- [x] 页眉渲染（YAML frontmatter 指定页眉内容）
- [x] 页脚渲染（YAML frontmatter 指定页脚内容）
- [x] 页码插入（页脚中的页码域）
- [x] 分栏渲染（多栏布局，通过 style.columns 配置）

**docx → md（反向提取）：**
- [x] 分栏检测（多栏布局 → 注释标记）

### Phase 13: 高级功能

**md → docx：**
- [ ] 目录生成（TOC）
- [ ] 书签插入
- [ ] 文字方向（RTL）
- [ ] 水印
- [ ] 文档保护/权限

**docx → md：**
- [ ] 目录还原（TOC 字段 → `[TOC]` 标记）
- [ ] 书签还原
- [ ] 文字方向检测

### Phase 14: glm-ocr 集成（可选 OCR 引擎）

- [x] 集成 ollama glm-ocr 作为可选 OCR 引擎
- [x] 支持 Text Recognition（纯文本输出）
- [x] 支持 Figure Recognition（HTML 表格输出）
- [x] CLI 参数选择 OCR 引擎（`--ocr-engine paddleocr|glm-ocr`）
- [x] 默认优先使用 glm-ocr（更准确），不可用时回退到 PaddleOCR

---

## 8. OCR — 图片文字提取 (Phase 8 + 14)

支持两个可选 OCR 引擎，均可独立安装使用：

### 引擎对比

| 特性 | PaddleOCR | glm-ocr (ollama) |
|------|-----------|------------------|
| 准确度 | 一般 | **更高** |
| 输出格式 | 纯文本 | Markdown / HTML 表格 |
| 安装方式 | pip install | ollama pull |
| 模型大小 | ~2.2GB | ~2.2GB |
| 优先级 | 可选 | **优先** |

**使用方式：**
```bash
# 命令行（默认使用 glm-ocr，不可用时回退到 PaddleOCR）
python -m md2word.cli.main input.docx -r --ocr

# 指定 OCR 引擎
python -m md2word.cli.main input.docx -r --ocr --ocr-engine glm-ocr
python -m md2word.cli.main input.docx -r --ocr --ocr-engine paddleocr

# docx2md 命令
docx2md input.docx --ocr --ocr-engine glm-ocr

# Python API
extractor = DocxExtractor(output_dir="output/images", ocr=True, ocr_engine="glm-ocr", max_ocr_images=10)
doc = extractor.extract("input.docx")
```

**输出格式：**
```markdown
![图片 5](images/image_001.png){:width=5.77in height=1.61in}

> OCR 文本：
>
> Metrics:
> 45705565088735095
```

**参数：**
- `ocr=True` — 启用 OCR
- `ocr_engine="glm-ocr"` 或 `"paddleocr"` — 选择引擎（默认 glm-ocr）
- `max_ocr_images=N` — 限制 OCR 处理的图片数量（0=不限制）

**依赖（可选）：**
```
# PaddleOCR（备选引擎）
paddleocr>=3.6.0
paddlepaddle>=3.0.0

# glm-ocr（优先引擎，通过 ollama 安装）
ollama pull glm-ocr
```

---

## 9. 依赖清单

```
mistune>=3.0
python-docx>=1.1.0
pywin32>=306         # Windows COM 操作 (win32com)
pyyaml>=6.0           # Frontmatter 解析
# docx→md 可选依赖
lxml>=4.9             # OMML 处理
latex2mathml>=3.77.0  # 公式转换
# OCR 可选依赖（图片文字提取）
# paddleocr>=3.6.0
# paddlepaddle>=3.0.0
```
